export const esc=x=>String(x??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));export const money=x=>Number(x||0).toLocaleString('en-US',{style:'currency',currency:'USD'});export function shell(title,active,body){return '<header class="top"><a class="brand" href="/rider" data-link>Raider Rides</a>'+['rider','driver','admin'].map(x=>'<a class="nav '+(x===active?'active':'')+'" href="/'+x+'" data-link>'+x[0].toUpperCase()+x.slice(1)+'</a>').join('')+'<span class="status">● API</span></header><main><p class="eyebrow">'+title+'</p>'+body+'</main>'}export const card=(t,b)=>'<section class="card"><h2>'+t+'</h2>'+b+'</section>';export const table=(hs,rs)=>'<div class="table-wrap"><table><thead><tr>'+hs.map(x=>'<th>'+x+'</th>').join('')+'</tr></thead><tbody>'+(rs.length?rs.map(r=>'<tr>'+r.map(x=>'<td>'+x+'</td>').join('')+'</tr>').join(''):'<tr><td colspan="'+hs.length+'" class="empty">No records.</td></tr>')+'</tbody></table></div>';export const toast=m=>{const x=document.createElement('div');x.className='toast';x.textContent=m;document.body.append(x);setTimeout(()=>x.remove(),3000)}