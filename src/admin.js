import { api } from "./api.js"; import { shell, card, table, esc, money, toast } from "./ui.js";
export async function renderAdmin(){const app=document.querySelector("#app");app.innerHTML=shell("Admin","admin",'<div id="stats" class="stats">Loading…</div><div class="grid">'+card("Rides",'<div id="adminRides">Loading…</div>')+card("Drivers",'<div id="drivers">Loading…</div>')+"</div>");try{const[s,r,d]=await Promise.all([api.admin.stats(),api.admin.rides(),api.admin.drivers()]);stats.innerHTML=[["Active rides",s.activeRides],["Online drivers",s.driversOnline],["Rides today",s.ridesToday],["Revenue",money(s.revenueToday)]].map(x=>'<div class="stat"><b>'+esc(x[1]??0)+"</b>"+x[0]+"</div>").join("");adminRides.innerHTML=table(["Status","Rider","Driver","Fare"],(r.rides||r||[]).map(x=>[esc(x.status),esc(x.riderName||"—"),esc(x.driverName||"—"),money(x.fare)]));drivers.innerHTML=table(["Driver","Status","Rating"],(d.drivers||d||[]).map(x=>[esc(x.name),x.online?"Online":"Offline",esc(x.rating??"—")]))}catch(e){toast(e.message);stats.innerHTML='<div class="empty">Admin API unavailable.</div>'}}